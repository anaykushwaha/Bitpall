{
  "name": "@bitpall/checker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src tests --max-warnings 0",
    "test": "vitest run"
  },
  "dependencies": {
    "@bitpall/ast": "workspace:*"
  },
  "devDependencies": {
    "@bitpall/parser": "workspace:*"
  }
}
